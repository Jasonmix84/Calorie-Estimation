# Calorie-Estimation

This is an iOS application project for calorie estimation, built using Swift and Xcode.

## 💻 Local Setup and Downloading

To get a copy of this project running on your local machine, you'll need Git installed.

### 1. Clone the Repository

Open your Terminal or command prompt and run the following command to clone the project:

```bash
git clone [https://github.com/Jasonmix84/Calorie-Estimation.git](https://github.com/Jasonmix84/Calorie-Estimation.git)
```

### 2. Navigate to the Project Directory

Change into the newly created project folder:

```bash
cd Calorie-Estimation
```

---

## 🛠 Dependencies

To successfully build and run this application, you must have the following software installed:

* **Xcode:** The official Integrated Development Environment (IDE) for macOS, used to build apps for Apple platforms. You can download it from the Mac App Store.
* **Swift:** The programming language used in this project. Xcode includes the necessary Swift toolchain.
* **macOS:** A computer running macOS is required to install and run Xcode.

---

## 🚀 Running the Program Using Xcode

Follow these steps to open the project and launch the application on a simulator or physical device:

### 1. Open the Project

Navigate to the project directory and open the main project file:

```bash
open Calorie-Estimator.xcodeproj
```

This command will launch the project in Xcode.

### 2. Select a Target

Once Xcode is open:

* In the top bar next to the **Run button (▶)**, click on the scheme selector (it might show the app name or a device name).
* Select an **iOS Simulator** (e.g., iPhone 15) or connect a physical iOS Device to your Mac and select it.

### 3. Build and Run

Click the **Run button (▶)** located in the upper-left corner of the Xcode window, or press **Cmd + R** on your keyboard.

Xcode will compile the code and install the application on the selected simulator or device. The app will launch automatically once the build is successful.
